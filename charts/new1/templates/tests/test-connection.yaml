apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "new1.fullname" . }}-test-connection"
  labels:
    {{- include "new1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "new1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
