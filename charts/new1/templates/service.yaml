apiVersion: v1
kind: Service
metadata:
  name: {{ include "new1.fullname" . }}
  labels:
    {{- include "new1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new1.selectorLabels" . | nindent 4 }}
